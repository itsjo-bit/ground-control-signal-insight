[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
# Exclude live API tests by default. Run with: pytest -m granite
addopts = -m "not granite"
markers =
    granite: marks tests that require a live IBM Granite API key (select with -m granite)